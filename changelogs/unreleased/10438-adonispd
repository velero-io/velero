`velero server` now derives its graceful shutdown timeout automatically from the pod's own `terminationGracePeriodSeconds` (minus a safety buffer), instead of always using controller-runtime's hardcoded 30s default. Add `--graceful-shutdown-timeout` to override the timeout explicitly, and `--graceful-shutdown-safety-buffer` to control the buffer subtracted during automatic derivation.
