Implement per-BSL encryption keys to enhance security by generating unique cryptographically random passwords for each BackupStorageLocation instead of using a single shared hardcoded password across all backups. This prevents a single compromised key from exposing all backup repositories. The implementation maintains backward compatibility with existing installations using the legacy shared key.
